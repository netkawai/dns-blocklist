# dns-blocklist
My personal DNS blocklist

Once update, I need to click Update All in the custom rules,
Otherwise, custom rules will not be applied.

And don't forget, I can update the custom rule (click Update All),
once per a hour, 5 times per a day. If I need to update bulk updates,
make sure all changes are in before clicking "Update All" and Max 99 domains.
